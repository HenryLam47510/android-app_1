name: test1
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  #(tạm thời tắt)
  #image_picker: ^1.0.7
  #camera: ^0.10.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  # Lưu ý: Không được lặp lại từ khóa 'flutter:' hay 'sdk: flutter' ở cuối file này nữa